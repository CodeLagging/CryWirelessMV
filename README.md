# Read me in progress!
## for now here are basic steps to get it working:
## Install libraries via ''pip install -r requirements.txt'' and it should work
## For IResp, the led+ (or your transistor's gate) should be on GPIO16
